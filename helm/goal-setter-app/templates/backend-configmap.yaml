apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goal-setter-app.fullname" . }}-backend-configmap

data:
  NODE-ENV: {{ .Values.backend.config.nodeEnv | quote }}
  PORT: {{ .Values.backend.config.port | quote }}
